/**
 * Shared asset paths for the assistant UI. Swap the files in `public/assets/`
 * to re-skin (keep the same names, or update the paths here).
 */

/** Circular headshot used in the chat header and empty state. */
export const AVATAR_SRC = 'assets/avatar.png';

/** Full-body illustrated character that leans on the docked chat window. */
export const CHARACTER_SRC = 'assets/character.png';

/** Desktop portal screenshot used as the background of the docked scene. */
export const DESKTOP_BG_SRC = 'assets/desktop.png';
